spring.application.name=backend

spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/parcezza}
spring.datasource.username=${POSTGRES_USER:parcezza}
spring.datasource.password=${POSTGRES_PASSWORD:parcezza}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.profiles.active=dev

security.jwt.secret=${JWT_SECRET:change-me-please-change-change-me-please-change}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
